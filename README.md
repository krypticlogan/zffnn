in development

## Benchmarks

Run the maintained operation benchmarks with:

```sh
zig build benchmark -Dop=relu -Doptimize=ReleaseFast
```

See [benchmarks/README.md](benchmarks/README.md) for the current performance
snapshot, methodology, coverage plan, and separately labeled historical data.
